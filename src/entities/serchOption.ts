export class SerchOption {
    gender:string
    grade:string
    major:string

    constructor(gender:string, grade:string, major:string) {
        this.gender = gender
        this.grade = grade
        this.major = major
    }
}