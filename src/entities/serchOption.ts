export class SerchOption {
    gender:string
    mode:string
    major:string

    constructor(gender:string, mode:string, major:string) {
        this.gender = gender
        this.mode = mode
        this.major = major
    }
}